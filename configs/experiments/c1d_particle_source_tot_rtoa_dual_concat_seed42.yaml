base: c1_particle_source_base.yaml

experiment_name: c1d_particle_source_tot_rtoa_dual_concat_seed42
experiment_group: c1_particle_source_baseline_seed42

dataset:
  modalities: [ToT, ToA]

data:
  toa_transform: relative_minmax

model:
  name: dual_stream_concat_aux
