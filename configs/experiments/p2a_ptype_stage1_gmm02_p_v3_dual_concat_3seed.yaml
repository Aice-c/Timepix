base: p2a_ptype_stage1_gmm02_p_v3_base.yaml

experiment_name: p2a_ptype_stage1_gmm02_p_v3_dual_concat

dataset:
  modalities: [ToT, ToA]

data:
  toa_transform: relative_minmax

model:
  name: dual_stream_concat_aux

grid:
  training.seed: [42, 43, 44]
