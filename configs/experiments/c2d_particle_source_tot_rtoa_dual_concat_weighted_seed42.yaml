base: c2_particle_source_weighted_base.yaml

experiment_name: c2d_particle_source_tot_rtoa_dual_concat_weighted_seed42
experiment_group: c2_particle_source_weighted_ce_seed42

dataset:
  modalities: [ToT, ToA]

data:
  toa_transform: relative_minmax

model:
  name: dual_stream_concat_aux
