base: p1b_ptype_stage1_full_am_co_sr_p_v1_base.yaml

experiment_name: p1d_ptype_stage1_full_am_co_sr_p_v1_gmu_lr_stability_base
experiment_group: p1d_ptype_stage1_full_am_co_sr_p_v1_gmu_lr_stability_seed42

dataset:
  modalities: [ToT, ToA]

data:
  toa_transform: relative_minmax

model:
  name: dual_stream_gmu_aux

training:
  epochs: 60
  early_stopping_patience: 20
