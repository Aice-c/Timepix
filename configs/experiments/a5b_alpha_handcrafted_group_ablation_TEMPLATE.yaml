# TEMPLATE ONLY. Fill handcrafted_features.features after A5a screening.
base: alpha_tot_a2_best_base.yaml

experiment_name: a5b_alpha_handcrafted_group_ablation
experiment_group: a5b_alpha_handcrafted_group_ablation

dataset:
  modalities: [ToT]

split:
  seed: 42
  reuse_split: true
  path: outputs/splits/Alpha_100_ToT-ToA_seed42_0.8_0.1_0.1.json

model:
  fusion_mode: concat

handcrafted_features:
  enabled: true
  standardize: true
  source_modalities: [ToT, ToA]
  # Fill with A5a-selected features before running.
  features: []

# Suggested post-A5a grid shape:
# - Main progression: Geometry -> Geometry+ToT -> Geometry+ToT+ToA
# - ToA-only is a side diagnostic, not the middle step of the progression.
# grid:
#   handcrafted_features.features:
#     - [selected_geometry_features]
#     - [selected_geometry_features, selected_tot_features]
#     - [selected_toa_axis_features]  # side diagnostic
#     - [selected_geometry_features, selected_tot_features, selected_toa_axis_features]
