# TEMPLATE ONLY.
# The official A5c seed42 diagnostic is:
#   configs/experiments/a5c_alpha_handcrafted_gated_seed42.yaml
# Keep this template only if a later write-up needs an explicit concat/gated
# fusion-mode comparison for a single selected feature group.
base: alpha_tot_a2_best_base.yaml

experiment_name: a5c_alpha_handcrafted_fusion_mode
experiment_group: a5c_alpha_handcrafted_fusion_mode

dataset:
  modalities: [ToT]

split:
  seed: 42
  reuse_split: true
  path: outputs/splits/Alpha_100_ToT-ToA_seed42_0.8_0.1_0.1.json

handcrafted_features:
  enabled: true
  standardize: true
  source_modalities: [ToT, ToA]
  # Fill with one selected A5 feature group before running.
  features: []

grid:
  model.fusion_mode:
    - concat
    - gated
